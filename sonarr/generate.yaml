# oapi-codegen configuration for Sonarr API client generation
package: sonarr
generate:
  client: true
  models: true
output: client.gen.go
output-options:
  skip-prune: true
  exclude-tags:
    - StaticResource
    - Authentication
    - ApiInfo
